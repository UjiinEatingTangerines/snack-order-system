{
  "name": "snack-order-system",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "prisma generate && next build",
    "start": "next start",
    "lint": "next lint",
    "vercel-build": "node scripts/set-version.js && prisma migrate deploy && prisma generate && next build",
    "postinstall": "prisma generate",
    "version:patch": "npm version patch",
    "version:minor": "npm version minor",
    "version:major": "npm version major",
    "db:reset": "tsx scripts/reset-db.ts",
    "cleanup:snacks": "tsx scripts/cleanup-old-snacks.ts"
  },
  "dependencies": {
    "@prisma/client": "5.14.0",
    "next": "^14.2.33",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.0.1",
    "postcss": "^8",
    "prisma": "5.14.0",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
